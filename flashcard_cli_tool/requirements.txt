# Korean Flashcard Processor - Requirements
# Minimal dependencies for standalone CLI tool

# Core dependencies
httpx>=0.25.0
pydantic>=2.5.0
python-dotenv>=1.0.0
typer[all]>=0.9.0
rich>=13.7.0

# Async support (built into Python 3.7+)
# asyncio is part of standard library